# DealWise currently uses GTK4 through system packages on Linux.
# Install with:
# sudo apt install python3-gi gir1.2-gtk-4.0
#
# Phase 2 uses only Python standard-library networking for the Vinted public connector.
# No pip dependencies are required yet.
